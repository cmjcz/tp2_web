{% extends 'base.html.twig' %}

{% block title %}Transactions{% endblock %}

{% block body %}
    <h1>Transactions</h1>

    <table>
        <tbody>
            <tr>
                <th scope="row">Idtransactions</th>
                <td>{{ transaction.idtransactions }}</td>
            </tr>
            <tr>
                <th scope="row">Solde</th>
                <td>{{ transaction.solde }}</td>
            </tr>
            <tr>
                <th scope="row">Date</th>
                <td>{{ transaction.date ? transaction.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('transactions_index') }}">back to list</a>

    <a href="{{ path('transactions_edit', {'idtransactions': transaction.idtransactions}) }}">edit</a>

    {{ include('transactions/_delete_form.html.twig') }}
{% endblock %}
