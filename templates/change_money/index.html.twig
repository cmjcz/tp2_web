{% extends 'base.html.twig' %}

{% block title %}{{ operation }}{% endblock %}

{% block body %}
	<div class="row">
    <h1>{{ operation }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="btn">{{ button_label|default("Valider") }}</button>
    {{ form_end(form) }}
	</div>
    <button onclick="window.location.href = '{{ path('comptes_show',  {'idcomptes' : compte.idcomptes}) }}';" type="button" class="btn red">Retour</button>
{% endblock %}
