<form method="post" action="{{ path('transactions_delete', {'idtransactions': transaction.idtransactions}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transaction.idtransactions) }}">
    <button class="btn">Delete</button>
</form>
