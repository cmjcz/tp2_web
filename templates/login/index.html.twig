{% extends 'base.html.twig' %}

{% block title %}Identification!{% endblock %}

{% block body %}
	<div class="row">
        <h1>Identification</h1>

        {{ form_start(formConnexion) }}
            {{ form_widget(formConnexion) }}
                <button class="waves-effect waves-light btn-large">
                	{{ button_label|default('Connexion') }} <i class="material-icons right">send</i>
                </button>
        {{ form_end(formConnexion) }}
    </div>    
    <button onclick="window.location.href = '{{ path('register') }}';" type="button" class="waves-effect waves-light btn">
    	Vous n'avez pas encore de compte ?
    </button>
{% endblock %}
